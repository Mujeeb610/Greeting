The purpose of the github repository

A GitHub Repository is a space for storing and managing source code for a given project. It is the most basic element of GitHub. It contains all the files, source code, documentation and other content required for the project.

What the Python script does?

print() is a built-in Python function used to display output.

"Hello Mujeeb" is a string (text) inside quotes.

When the script runs, it outputs: Hello Mujeeb

Python reads the line from left to right.

It sees the print function.

It takes the string "Hello Mujeeb" as input.

It displays that text on the screen.
